test.py --> da aggiustare (è il codice che implementa la logica di Untitled1 in pyspark) --> togli roba inutile e metti minimo essenziale per farlo girare (togli ALS)
crea notebook di SVD_sanity_check.ipynb dove confrontreremo (immagini etc) 
convertire in spark svd.ipynb (ma non la parte di sanity check) --> di nuovo stretto necessario per arrivare a eseguire
se riesci a vedere perché fare root di Sigma in SVD e perché moltiplicare Sigma per U e V